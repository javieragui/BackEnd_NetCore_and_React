import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette : {
        primary : {
            light : "#5e92f3",
            main : "#1565c0",
            dark : "#2196f3",
            contrastText : "#ecfad8"
        }
    }
});
export default theme;