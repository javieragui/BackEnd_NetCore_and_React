import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette : {
        primary : {
            light : "#ab47bc",
            main : "#ab47bc",
            dark : "#df78ef",
            contrastText : "#ecfad8"
        }
    }
});
export default theme;